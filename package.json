{
  "name": "lore",
  "version": "0.4.1",
  "private": true,
  "type": "module",
  "description": "Repo living-docs / decision-history framework — capture (hook/mine/note/docs) → synthesize (4-axis wiki: component/theme/flow/docs) → consume (ask/serve)",
  "scripts": {
    "test": "node --test"
  }
}
