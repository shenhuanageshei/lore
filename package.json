{
  "name": "lore",
  "version": "0.8.2",
  "private": true,
  "type": "module",
  "description": "Repo living-docs / decision-history framework — capture → synthesize (HOME + multi-axis wiki, bilingual) → consume (human read / agent retrieval)",
  "scripts": {
    "test": "node --test",
    "version": "node scripts/sync-version.mjs && git add .claude-plugin/plugin.json"
  }
}
